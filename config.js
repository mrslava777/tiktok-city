'use strict';

/**
 * ВСЕ НАСТРОЙКИ ИГРЫ «King of Live».
 * Меняй значения здесь — балансировка урона, HP, награды, эффектов.
 */
module.exports = {
  // TikTok username лайва по умолчанию (без @). Можно оставить пустым
  // и вводить прямо в интерфейсе, либо задать через переменную окружения.
  TIKTOK_USERNAME: process.env.TIKTOK_USERNAME || '',

  // ── КОРОЛЬ ─────────────────────────────────────────────────────────
  KING_HP: 10000,        // стартовый запас здоровья нового Короля
  BOUNTY_BASE: 1000,     // награда за голову в момент коронации

  // Награда за голову растёт со временем на троне (в секундах → очки).
  // Между точками значение плавно интерполируется (растёт каждые ~30с).
  BOUNTY_SCHEDULE: [
    [0, 1000],
    [60, 2000],     // 1 минута
    [180, 5000],    // 3 минуты
    [300, 10000],   // 5 минут
    [600, 25000],   // 10 минут
    [1200, 100000], // 20 минут
  ],

  // ── УРОН ПО ПОДАРКАМ ───────────────────────────────────────────────
  // Сначала ищем точное имя подарка (byName), иначе считаем по стоимости
  // в «алмазах» (byDiamondTier). repeatCount умножает урон.
  GIFT_DAMAGE: {
    byName: {
      rose: 10,
      роза: 10,
      heart: 50,
      'heart me': 50,
      'i love you': 50,
      tiktok: 100,
      'tiktok logo': 100,
    },
    byDiamondTier: [
      { maxDiamonds: 1, damage: 10 },          // Rose
      { maxDiamonds: 5, damage: 50 },          // Heart
      { maxDiamonds: 20, damage: 100 },        // TikTok
      { maxDiamonds: 99, damage: 250 },        // средние подарки
      { maxDiamonds: 999, damage: 1000 },      // большие подарки
      { maxDiamonds: Infinity, damage: 5000 }, // очень дорогие подарки
    ],
    default: 10,
  },

  // Урон за лайки (TikTok шлёт лайки пачками: итог = LIKE_DAMAGE * likeCount).
  // Поставь 0, чтобы лайки не наносили урон.
  LIKE_DAMAGE: 5,

  // ── ЗАЩИТА КОРОЛЯ (Король дарит подарок сам себе → лечение) ─────────
  HEAL_BY_TIER: [
    { maxDiamonds: 5, hp: 20 },        // Rose / мелкий → +20 HP
    { maxDiamonds: 99, hp: 500 },      // средний → +500 HP
    { maxDiamonds: Infinity, hp: 2000 }, // большой+ → +2000 HP
  ],

  // ── КРИТИЧЕСКИЕ УДАРЫ ──────────────────────────────────────────────
  CRIT: {
    chance: 0.12,   // вероятность крита на удар
    x5Chance: 0.25, // доля x5 среди критов (остальное x2)
  },

  // ── СПЕЦИАЛЬНЫЕ ЭФФЕКТЫ ────────────────────────────────────────────
  SHIELD_MS: 15000,        // Щит: иммунитет 15 секунд
  BERSERK_MS: 20000,       // Берсерк: длительность
  BERSERK_MULT: 2,         // Берсерк: урон охотников x2
  HEAL_PERCENT: 0.20,      // Лечение: восстановить 20% от максимума HP

  // ── СЛУЧАЙНЫЕ СОБЫТИЯ ТРОНА ────────────────────────────────────────
  RANDOM_EVENT_INTERVAL_MS: 150000, // примерно раз в 2,5 минуты
  EVENT_DMG_MS: 30000,              // длительность усиления урона (Восстание/Час охоты)
  EVENT_DMG_MULT: 2,                // множитель урона на событии
  EVENT_DOUBLE_BOUNTY_MS: 60000,    // длительность ускорения награды
  EVENT_DOUBLE_BOUNTY_MULT: 2,      // во сколько раз быстрее растёт награда
};
